import * as React from 'react'
import { FC } from 'react'      /* FC = FunctionComponent = Function that accepts a single
                                “props” (which stands for properties) object argument with 
                                data and returns a React element */

const HelloTypeScript: FC = () => <h1>Hello, TypeScript!</h1>
export default HelloTypeScript